#include <iostream>
#include <cmath>
using namespace std;

int main () {
    int x = 5, y = 10;
    cout << max(x, y) << endl; 
    cout << min(x, y) << endl; 

    cout << sqrt (81) << endl;
    cout << round (2.9) << endl; 
    cout << log (3) << endl;
    return 0;
}